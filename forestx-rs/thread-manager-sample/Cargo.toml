[package]
name = "forestx-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Forestx workspace dependency.
# Add new Forestx surface area to `forestx-core-api` instead of depending on
# additional `forestx-*` crates here.
forestx-core-api = { workspace = true }
tracing = { workspace = true }
