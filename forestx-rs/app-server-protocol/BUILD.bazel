load("//:defs.bzl", "forestx_rust_crate")

forestx_rust_crate(
    name = "app-server-protocol",
    compile_data = glob(["schema/precomputed/**"]),
    crate_name = "forestx_app_server_protocol",
    test_data_extra = glob(
        ["schema/**"],
        allow_empty = True,
    ),
)
