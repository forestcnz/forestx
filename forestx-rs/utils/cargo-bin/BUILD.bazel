load("//:defs.bzl", "forestx_rust_crate")

exports_files(
    ["repo_root.marker"],
    visibility = ["//visibility:public"],
)

forestx_rust_crate(
    name = "cargo-bin",
    compile_data = ["repo_root.marker"],
    crate_name = "forestx_utils_cargo_bin",
    lib_data_extra = ["repo_root.marker"],
    rustc_env = {
        "FORESTX_REPO_ROOT_MARKER": "$(rlocationpath :repo_root.marker)",
    },
    test_data_extra = ["repo_root.marker"],
)
