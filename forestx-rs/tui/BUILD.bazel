load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "forestx_rust_crate")

forestx_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//forestx-rs/collaboration-mode-templates:templates/default.md",
        "//forestx-rs/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "forestx_tui",
    extra_binaries = [
        "//forestx-rs/cli:forestx",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
