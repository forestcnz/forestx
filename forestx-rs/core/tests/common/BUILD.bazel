load("//:defs.bzl", "forestx_rust_crate")

forestx_rust_crate(
    name = "common",
    crate_name = "core_test_support",
    crate_srcs = glob(["*.rs"]),
    deps_extra = [
        "@crates//:similar",
    ],
)
