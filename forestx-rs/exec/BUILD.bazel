load("//:defs.bzl", "forestx_rust_crate")

forestx_rust_crate(
    name = "exec",
    crate_name = "forestx_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
