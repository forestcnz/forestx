load("//:defs.bzl", "forestx_rust_crate")

forestx_rust_crate(
    name = "core-skills",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "forestx_core_skills",
)
